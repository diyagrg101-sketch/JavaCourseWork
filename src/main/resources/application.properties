# ============================================
# Sip and Serve - Application Configuration
# ============================================

# Database
db.url=jdbc:mysql://localhost:3306/sipserve_db?useSSL=false&serverTimezone=UTC
db.username=root
db.password=
db.driver=com.mysql.cj.jdbc.Driver

# Application
app.name=Sip and Serve
app.version=1.0.0
app.upload.dir=uploads
